/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 'i.pinimg.com', 'www.shutterstock.com', 'img.archilovers.com']
    }
}

module.exports = nextConfig
